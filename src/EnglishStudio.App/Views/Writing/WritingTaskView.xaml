<UserControl x:Class="EnglishStudio.App.Views.Writing.WritingTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EnglishStudio.App.ViewModels.Writing"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:WritingTaskViewModel}"
             d:DesignHeight="700" d:DesignWidth="1100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header bar -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="‹ Отмена"
                    Style="{StaticResource FlatButton}"
                    Command="{Binding CancelCommand}" />

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="{Binding KindLabel}" Style="{StaticResource H2}" />
                <TextBlock Text="  ·  " Style="{StaticResource MutedText}" />
                <TextBlock Text="{Binding Title}" Style="{StaticResource MutedText}" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{DynamicResource OverlayLight13Brush}" CornerRadius="6" Padding="10,4" Margin="0,0,8,0">
                    <TextBlock Foreground="{DynamicResource StrongTextBrush}" FontWeight="SemiBold">
                        <Run Text="⏱ " /><Run Text="{Binding ElapsedDisplay, Mode=OneWay}" />
                    </TextBlock>
                </Border>
                <Border Background="{DynamicResource OverlayLight13Brush}" CornerRadius="6" Padding="10,4">
                    <TextBlock Foreground="{DynamicResource StrongTextBrush}">
                        <Run Text="Рекомендация: " />
                        <Run Text="{Binding RecommendedMinutes, Mode=OneWay}" /><Run Text=" мин" />
                    </TextBlock>
                </Border>
            </StackPanel>
        </Grid>

        <!-- Body: 2 columns -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" MinWidth="380" />
                <ColumnDefinition Width="1*" MinWidth="380" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,8,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <TextBlock Text="Задание" Style="{StaticResource H3}" Margin="0,0,0,8" />
                        <TextBlock Text="{Binding PromptText}"
                                   Style="{StaticResource BodyText}"
                                   TextWrapping="Wrap" />
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="Ваш ответ" Style="{StaticResource H3}" Margin="0,0,0,8" />
                    <TextBox Grid.Row="1"
                             Text="{Binding UserText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource FlatTextBox}"
                             AcceptsReturn="True"
                             AcceptsTab="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             SpellCheck.IsEnabled="True"
                             Language="en-GB"
                             VerticalContentAlignment="Top" />
                </Grid>
            </Border>
        </Grid>

        <!-- Bottom bar -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Background="{DynamicResource OverlayLight13Brush}" CornerRadius="6" Padding="10,4" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Foreground="{DynamicResource StrongTextBrush}" FontWeight="SemiBold">
                            <Run Text="{Binding WordCount, Mode=OneWay}" />
                            <Run Text=" / " />
                            <Run Text="{Binding MinWords, Mode=OneWay}" />
                            <Run Text=" слов" />
                        </TextBlock>
                    </StackPanel>
                </Border>
                <TextBlock Style="{StaticResource MutedText}"
                           Text="{Binding StatusText}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center" />
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Сохранить черновик"
                        Style="{StaticResource FlatButton}"
                        Margin="0,0,8,0"
                        Command="{Binding SaveDraftCommand}" />
                <Button Content="Отправить на проверку"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding SubmitCommand}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
