<UserControl x:Class="EnglishStudio.App.Views.Writing.WritingModuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EnglishStudio.App.ViewModels.Writing"
             xmlns:views="clr-namespace:EnglishStudio.App.Views.Writing"
             xmlns:controls="clr-namespace:EnglishStudio.App.Views.Controls"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:WritingHubViewModel}"
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <DataTemplate DataType="{x:Type vm:WritingResultScreen}">
            <views:WritingResultView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:WritingHistoryViewModel}">
            <views:WritingHistoryView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <views:WritingHubView Visibility="{Binding IsHubVisible, Converter={StaticResource BoolToVis}}" />
        <ContentControl Content="{Binding CurrentScreen}" />
        <controls:ContentMissingView
            Visibility="{Binding IsContentMissing, Converter={StaticResource BoolToVis}}"
            ImportCommand="{Binding OpenImportCommand}"
            MessageText="{Binding ContentMissingText}" />
    </Grid>
</UserControl>
