<UserControl x:Class="EnglishStudio.App.Views.ReadingStudy.ReadingSpeedChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="240" d:DesignWidth="560"
             Background="Transparent"
             SizeChanged="OnSizeChanged">
    <Grid>
        <Canvas x:Name="PlotCanvas" ClipToBounds="True" />
        <TextBlock x:Name="EmptyLabel"
                   Text="Нет данных — прочитайте вслух хотя бы один текст."
                   Visibility="Collapsed"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{DynamicResource MutedTextBrush}"
                   FontSize="13" />
    </Grid>
</UserControl>
