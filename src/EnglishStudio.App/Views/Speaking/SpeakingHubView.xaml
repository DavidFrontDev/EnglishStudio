<UserControl x:Class="EnglishStudio.App.Views.Speaking.SpeakingHubView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:EnglishStudio.App.ViewModels.Speaking"
             xmlns:loc="clr-namespace:EnglishStudio.App.Localization"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:SpeakingHubViewModel}"
             d:DesignHeight="700" d:DesignWidth="1100">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />

        <Style x:Key="ModePill" TargetType="RadioButton">
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Foreground" Value="{DynamicResource StrongTextBrush}" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bg"
                                Background="{DynamicResource OverlayLight13Brush}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource OverlayLight20Brush}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{DynamicResource AccentMidBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" MinWidth="540" />
            <ColumnDefinition Width="1*" MinWidth="280" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: mode + topic + start -->
        <Grid Grid.Column="0" Margin="0,0,12,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock Text="🎤 IELTS Speaking" Style="{StaticResource H1}" />
                <TextBlock Text="{Binding ModeDescription}"
                           Style="{StaticResource MutedText}"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0" />

                <!-- AI unavailable warning -->
                <Border Background="{DynamicResource OverlayLight13Brush}"
                        BorderBrush="{DynamicResource WarningBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="10,8"
                        Margin="0,10,0,0"
                        Visibility="{Binding IsAiUnavailable, Converter={StaticResource BoolToVis}}">
                    <TextBlock Text="{loc:Tr Hub_AiUnavailable}"
                               Style="{StaticResource MutedText}"
                               Foreground="{DynamicResource WarningBrush}"
                               TextWrapping="Wrap" />
                </Border>
            </StackPanel>

            <!-- Mode toggle -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                <RadioButton GroupName="Mode"
                             Style="{StaticResource ModePill}"
                             Content="Full mock"
                             IsChecked="{Binding SelectedMode, Converter={StaticResource EnumBoolConverter}, ConverterParameter=FullMock}" />
                <RadioButton GroupName="Mode"
                             Style="{StaticResource ModePill}"
                             Content="Part 1"
                             IsChecked="{Binding SelectedMode, Converter={StaticResource EnumBoolConverter}, ConverterParameter=Part1Only}" />
                <RadioButton GroupName="Mode"
                             Style="{StaticResource ModePill}"
                             Content="Part 2"
                             IsChecked="{Binding SelectedMode, Converter={StaticResource EnumBoolConverter}, ConverterParameter=Part2Only}" />
                <RadioButton GroupName="Mode"
                             Style="{StaticResource ModePill}"
                             Content="Part 3"
                             IsChecked="{Binding SelectedMode, Converter={StaticResource EnumBoolConverter}, ConverterParameter=Part3Only}" />
            </StackPanel>

            <!-- Topic filter (search box) -->
            <Border Grid.Row="2"
                    Background="{DynamicResource OverlayLight13Brush}"
                    CornerRadius="8" Padding="10,6" Margin="0,0,0,8">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="🔎"
                               FontSize="14"
                               Margin="0,0,8,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource StrongTextBrush}" />
                    <TextBox Width="320"
                             Style="{StaticResource FlatTextBox}"
                             Text="{Binding TopicFilter, UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </Border>

            <!-- Topic list / random hint -->
            <Border Grid.Row="3" Style="{StaticResource Card}">
                <Grid>
                    <TextBlock VerticalAlignment="Center"
                               HorizontalAlignment="Center"
                               TextAlignment="Center"
                               Style="{StaticResource MutedText}"
                               TextWrapping="Wrap"
                               Margin="20"
                               Visibility="{Binding ShowsRandomNote, Converter={StaticResource BoolToVis}}"
                               Text="{loc:Tr Speaking_RandomTopicHint}" />

                    <ListBox ItemsSource="{Binding Part1TopicsView}"
                             SelectedItem="{Binding SelectedPart1Topic, Mode=TwoWay}"
                             Style="{StaticResource FlatListBox}"
                             ItemContainerStyle="{StaticResource AnimatedSelectionListBoxItem}"
                             Visibility="{Binding ShowsPart1Picker, Converter={StaticResource BoolToVis}}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,6">
                                    <TextBlock Text="{Binding TopicLabel}"
                                               FontWeight="SemiBold"
                                               FontSize="14"
                                               Foreground="{DynamicResource StrongTextBrush}" />
                                    <TextBlock Style="{StaticResource Caption}" Margin="0,2,0,0">
                                        <Run Text="{Binding QuestionCount, Mode=OneWay}" />
                                        <Run Text="{loc:Tr Speaking_Questions}" />
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <ListBox ItemsSource="{Binding Part2TopicsView}"
                             SelectedItem="{Binding SelectedPart2Topic, Mode=TwoWay}"
                             Style="{StaticResource FlatListBox}"
                             ItemContainerStyle="{StaticResource AnimatedSelectionListBoxItem}"
                             Visibility="{Binding ShowsPart2Picker, Converter={StaticResource BoolToVis}}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,6">
                                    <TextBlock Text="{Binding TopicLabel}"
                                               FontWeight="SemiBold"
                                               FontSize="14"
                                               Foreground="{DynamicResource StrongTextBrush}" />
                                    <TextBlock Style="{StaticResource Caption}"
                                               Text="{Binding CueCardPrompt}"
                                               TextTrimming="CharacterEllipsis"
                                               TextWrapping="Wrap"
                                               MaxHeight="32"
                                               Margin="0,2,0,0" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <TextBlock Grid.Row="4"
                       Style="{StaticResource MutedText}"
                       Text="{Binding StatusText}"
                       TextWrapping="Wrap"
                       Margin="0,8,0,0" />

            <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,14,0,0" HorizontalAlignment="Right">
                <Button Content="{loc:Tr Speaking_Start}"
                        Style="{StaticResource AccentButton}"
                        Command="{Binding StartCommand}" />
            </StackPanel>
        </Grid>

        <!-- RIGHT: history -->
        <Grid Grid.Column="1" Margin="12,0,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,8,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{loc:Tr Speaking_History}" Style="{StaticResource H2}" />
                <Button Grid.Column="1"
                        Content="{loc:Tr Speaking_Clear}"
                        Style="{StaticResource FlatButton}"
                        Padding="10,4"
                        FontSize="12"
                        ToolTip="{loc:Tr Speaking_ClearHistoryTooltip}"
                        Command="{Binding ClearHistoryCommand}" />
            </Grid>

            <Border Grid.Row="1" Style="{StaticResource Card}">
                <ListBox ItemsSource="{Binding RecentAttempts}"
                         Style="{StaticResource FlatListBox}"
                         ItemContainerStyle="{StaticResource FlatListBoxItem}">
                    <ListBox.InputBindings>
                        <MouseBinding MouseAction="LeftDoubleClick"
                                      Command="{Binding OpenAttemptCommand}"
                                      CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=ListBox}, Path=SelectedItem}" />
                    </ListBox.InputBindings>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,2">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Mode}" FontWeight="SemiBold" />
                                </StackPanel>
                                <TextBlock Style="{StaticResource Caption}" Margin="0,2,0,0">
                                    <Run Text="Band " /><Run Text="{Binding BandOverall, StringFormat='F1', Mode=OneWay, FallbackValue='—'}" />
                                    <Run Text="  ·  " />
                                    <Run Text="{Binding StartedAt, StringFormat='dd.MM HH:mm', Mode=OneWay}" />
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>
    </Grid>
</UserControl>
